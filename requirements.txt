# Reproducing this repo
# -----------------------------------------------------------------------------
# Thesis runs were produced on PyTorch 2.10.0 + CUDA 12.8 on an NVIDIA T4.
# The version floors below cover the APIs the notebooks actually use:
#   - torch.amp.autocast / torch.amp.GradScaler  (torch >= 2.1)
#   - torchvision.models.video multi-weight API  (torchvision >= 0.16)
# A current stable wheel of torch+torchvision works fine.

torch>=2.1
torchvision>=0.16

numpy>=1.24
pandas>=2.0
opencv-python>=4.8
scikit-learn>=1.3
tqdm>=4.65

# SoccerNet split list + dataset downloader (uses NV_PASSWORD env var)
SoccerNet>=0.1.55

# Notebook runtime
jupyter>=1.0
ipykernel>=6.25

# Optional — training logging
wandb>=0.16

# Plots
matplotlib>=3.7
seaborn>=0.12
